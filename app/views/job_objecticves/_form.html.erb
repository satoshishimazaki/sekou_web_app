<%= form_with(model: job_objecticfe, local: true) do |form| %>
  <% if job_objecticfe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_objecticfe.errors.count, "error") %> prohibited this job_objecticfe from being saved:</h2>

      <ul>
      <% job_objecticfe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
