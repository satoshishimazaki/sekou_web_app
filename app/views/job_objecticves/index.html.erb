<p id="notice"><%= notice %></p>

<h1>Job Objecticves</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_objecticves.each do |job_objecticfe| %>
      <tr>
        <td><%= job_objecticfe.name %></td>
        <td><%= link_to 'Show', job_objecticfe %></td>
        <td><%= link_to 'Edit', edit_job_objecticfe_path(job_objecticfe) %></td>
        <td><%= link_to 'Destroy', job_objecticfe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Job Objecticfe', new_job_objecticfe_path %>
